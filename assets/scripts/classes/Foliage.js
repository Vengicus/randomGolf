'use strict';

var app = app || {};

function Foliage(position, radius, treeNum)
{
    this.position = position;
    this.radius = radius;
    this.treeNum = treeNum;
}
Foliage.prototype.buildTree = function()
{
    
}
Foliage.prototype.buildFoliage = function()
{
    
}
Foliage.prototype.displayFoliage = function()
{
    
}